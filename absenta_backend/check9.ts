import { PrismaClient } from '@prisma/client'; const prisma = new PrismaClient(); async function main() { const c = await prisma.organizationalCapability.findFirst({ where: { position: { tenant_id: 'aa8847c6-c265-4086-b786-99116a6b9d75', code: 'KEPALA_SEKOLAH' } } }); console.log('Cimahi KEPALA_SEKOLAH caps created_at:', c?.created_at); } main().finally(() => prisma.$disconnect());