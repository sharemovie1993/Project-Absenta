module.exports = {
  apps: [
    {
      name: 'absenta-SMART-dev',
      script: 'src/main.ts',
      interpreter: 'node',
      interpreter_args: '-r ts-node/register -r tsconfig-paths/register',
      cwd: './',
      exec_mode: 'fork',
      instances: 1,
      env: {
        NODE_ENV: 'development',
        EMBEDDED_WORKERS: 'true',
        HIGHEST_LEVEL_DEBUG: 'true'
      },
      watch: ['src'],
      ignore_watch: ['node_modules', 'dist', 'logs', 'storage', 'uploads'],
      max_memory_restart: '1G'
    }
  ]
};
