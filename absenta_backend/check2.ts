import { PrismaClient } from '@prisma/client'; const prisma = new PrismaClient(); async function main() { const positions = await prisma.organizationalPosition.findMany(); console.log(positions.map(p => ({id: p.id, tenant: p.tenant_id, code: p.code, name: p.name}))); const caps = await prisma.organizationalCapability.findMany(); console.log(caps.slice(0, 5));} main().finally(() => prisma.$disconnect());