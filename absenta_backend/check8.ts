import { PrismaClient } from '@prisma/client'; const prisma = new PrismaClient(); async function main() { const positions = await prisma.organizationalPosition.findMany({ where: { tenant_id: 'aa8847c6-c265-4086-b786-99116a6b9d75' } }); for (const p of positions) { const caps = await prisma.organizationalCapability.findMany({ where: { position_id: p.id }, select: { permission_id: true } }); console.log(p.code, caps.map(c => c.permission_id)); } } main().finally(() => prisma.$disconnect());